# Expo + React Native 生态下，Metro 在解析 expo-router 内部的 import 时会穿过
# pnpm 默认的 .pnpm 隔离层，搜不到兄弟 peer dep（whatwg-fetch / react-native-web
# / react-native-css-interop / @expo/metro-runtime 等）。一劳永逸：用 hoisted
# linker，让整个 workspace 的 node_modules 像 npm 一样扁平。
#
# 对 apps/web (Next.js) 和 packages/shared (tsup) 均无副作用；Next.js 本身不依赖
# pnpm 的隔离语义。
node-linker=hoisted
